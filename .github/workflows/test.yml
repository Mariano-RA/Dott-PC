name: Publish python-api (buildx)

on:
  push:
    branches: ["main"]
    paths:
      - "python-api/**"
      - ".github/workflows/python-ci.yml"
  workflow_dispatch: {}

env:
  DOCKERHUB_NAMESPACE: marianora
  IMAGE_NAME: dott-python
  DOCKERFILE_DIR: python-api

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    concurrency:
      group: python-api-build
      cancel-in-progress: true

    steps:
      - uses: actions/checkout@v4

      - name: Set up Docker Buildx (pinned BuildKit)
        uses: docker/setup-buildx-action@v3
        with:
          driver-opts: image=moby/buildkit:latest
          buildkitd-flags: --debug

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Docker metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.DOCKERHUB_NAMESPACE }}/${{ env.IMAGE_NAME }}
          tags: |
            type=raw,value=latest
            type=sha,format=long

      - name: Build & Push (Buildx + cache, sin provenance)
        uses: docker/build-push-action@v6
        with:
          context: ${{ env.DOCKERFILE_DIR }}
          file: ${{ env.DOCKERFILE_DIR }}/Dockerfile
          push: true
          platforms: linux/amd64
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
          pull: true
          provenance: false   # ← clave
          sbom: false         # ← opcional, también desactiva SBOM
